--testString = "LuaBridge works!" // this is how u comment out

--do u want to see cool stuff?
isEnabled = false

--testing LuaReset() in LuaTest.h
LuaResetX = 150.0
LuaResetY =  50.0

--testing LuaPrint() in LuaTest.h
--calling c++ functions in Lua
--testFn = function()
--	if (isEnabled) then
--		LuaPrint("GO TETRAIDERS!!!!")
--	return 0;
--end
